<%= form_with(model: geral_marca, local: true) do |form| %>
  <% if geral_marca.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(geral_marca.errors.count, "error") %> prohibited this geral_marca from being saved:</h2>

      <ul>
      <% geral_marca.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
